#!/usr/bin/env python
import sys
import os
sys.path.append(os.path.dirname(os.path.realpath(__file__)) + "/../src")

from directories_preparation.create_directory import create_dir

create_dir(['features', 'steps', 'pages', 'resources_for_testing'])
# paths = ['features', 'steps', 'pages', 'resources_for_testing']
#
# # Check whether the specified path exists or not
# for path in paths:
#
#     is_exist = os.path.exists(path)
#     if not is_exist:
#         # Create a new directory because it does not exist
#         os.makedirs(path)
#         print(f"The directory {path} was created!")
#     else:
#         print(f"The directory {path} already exists!")
